# HSAOI_Demo 代码修改记录

本文件为本仓库的整体代码修改记录，按时间从旧到新追加。
记录格式参考约定：每条至少包含 ①修改的文件 ②修改内容 ③修改思路或原因 ④是否已测试或验证。

---

## 2026年7月10日 - 项目改名（HSAOI → AOI，删除 Haoshi）

1. **修改的文件**
   - `HSAOI.pro` → 重命名为 `AOI.pro`，并修改文件内容
   - `HSAOI_zh_CN.ts` → 重命名为 `AOI_zh_CN.ts`，并修改文件内容
   - `HSAOI.pro.user`、`HSAOI.pro.user.0d77ab8`、`HSAOI.pro.user.aa22503`、`HSAOI.pro.user.aa64370`、`HSAOI.pro.user.cbb74ef` → 全部重命名为对应 `AOI.pro.user*`，并修改文件内容
   - `README.md`

2. **修改内容**
   - 全仓库（第三方 `HalconLib/` 目录除外）内出现的 `HSAOI` 全部改为 `AOI`：
     - `AOI.pro`：`@projectname` 由 `HSAOI` 改为 `AOI`；`TRANSLATIONS` 引用改为 `AOI_zh_CN.ts`。
     - `AOI_zh_CN.ts`：`<TS language="HSAOI_zh_CN">` 改为 `AOI_zh_CN`。
     - `AOI.pro.user*`：内部 `.pro` 引用、`HSAOI2` 显示名、`HSAOI_Debug/HSAOI_Release` 等构建目录名统一替换为 `AOI*`。
     - `README.md`：标题 `HSAOI_Demo` 改为 `AOI_Demo`；更新记录表内所有历史版本号 `HSAOI_xxxx` 改为 `AOI_xxxx`（按用户要求历史标签一并统一）。
   - 删除 `Haoshi`（安徽豪视）相关信息：`AOI.pro` 头部注释中的 `@Company  Anhui Haoshi` 整行删除，不保留公司署名。

3. **修改思路或原因**
   - 应用户要求对项目去除原公司标识：将项目名从 `HSAOI` 统一改为通用的 `AOI`，并移除 `Haoshi` 公司信息。
   - 第三方 Halcon 库（`HalconLib/`，头文件均以 `H` 开头）与本次改名无关，保持原样不动，避免破坏库依赖。

4. **是否已测试或验证**
   - 已用全仓库检索确认：除 `HalconLib/` 外，无任何 `HSAOI` / `Haoshi` 残留。
   - 文件重命名通过 `git mv` 完成，保留版本历史。
   - 未进行 Qt 编译验证（本环境无 Qt/Halcon 构建工具链）。
